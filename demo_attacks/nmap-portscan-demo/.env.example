TARGET_IP=10.51.1.12

# Use enough ports to trigger cardinality-based port scan detection.
SCAN_PORTS=1-500

# connect = -sT, reliable for Zeek demo
# syn = -sS, closer to classic Nmap SYN scan but may need raw socket capability
SCAN_TYPE=connect

OUTPUT_DIR=/results
